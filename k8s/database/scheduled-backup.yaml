apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: controlsystem-backup
  namespace: control-system
spec:
  cluster: 
    name: controlsystem-pg
  schedule: "0 0 23 * * *"
  method: plugin          
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
