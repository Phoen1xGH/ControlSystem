apiVersion: operator.victoriametrics.com/v1beta1
kind: VMPodScrape
metadata:
  name: controlsystem-pg
  namespace: control-system
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: controlsystem-pg
  podMetricsEndpoints:
    - port: metrics
